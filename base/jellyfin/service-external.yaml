# Same NAS-hosted Jellyfin as service.yaml, fronted separately for the
# telheira.tplinkdns.com routes. No selector - see endpointslice.yaml.
# Fixed 2026-07-10, see service.yaml for context.
apiVersion: v1
kind: Service
metadata:
  name: jellyfin-external
  labels:
    app: media
spec:
  ports:
  - port: 8096
    targetPort: 8096
    name: http
