apiVersion: v1
kind: Service
metadata:
  name: radarr
  labels:
    app: media
spec:
  ports:
  - port: 7878
    protocol: TCP
    targetPort: 7878
    name: webui
  selector:
    app: media
    run: radarr
