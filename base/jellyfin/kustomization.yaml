apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# deployment.yaml is intentionally NOT included: there is no live Jellyfin
# Deployment in the cluster. The "jellyfin" and "jellyfin-external" Services
# both currently have zero endpoints - kept here only because they exist
# live, not because Jellyfin is actually reachable through them right now.
resources:
- service.yaml
- service-external.yaml
