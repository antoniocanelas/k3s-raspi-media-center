apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# deployment.yaml is intentionally NOT included: Jellyfin runs on the
# Synology NAS (192.168.0.200), not as a Deployment in this cluster.
# See service.yaml and endpointslice.yaml.
resources:
- service.yaml
- service-external.yaml
- endpointslice.yaml
